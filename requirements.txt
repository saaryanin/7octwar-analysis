# Core data handling
pandas>=2.2.0
requests>=2.32.0

# For database export (optional but very useful with DBeaver)
sqlalchemy>=2.0.0

# For Excel export support (pandas uses it automatically)
openpyxl>=3.1.0

# Visualization (good for quick exploration before Tableau)
matplotlib>=3.9.0
seaborn>=0.13.0

# Optional but nice for interactive dashboards / exploration
plotly>=5.24.0

# For better data type handling and progress bars
tqdm>=4.66.0
python-dateutil>=2.9.0